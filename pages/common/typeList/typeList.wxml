<template name="typeList">
  <view class='type'>
    <block>
      <view wx:for="{{typeLists}}" wx:key="index">
        <text data-index="{{index}}" catchtap='chooseType' class="{{active==index?'active':''}}">{{item.name}}</text>
      </view>
    </block>
  </view>
</template>