<template name="typeList">
  <view class='type'>
    <block>
      <view wx:for="{{number}}" wx:key="index">
        <text data-index="{{index}}" catchtap='chooseType' class="{{active==index?'active':''}}">支部大会</text>
      </view>
    </block>
  </view>
</template>