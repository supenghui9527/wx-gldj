<!--pages/mine/myPosts/myPosts.wxml-->
<view>
  <postingsList isShowFouse="{{isShowFouse}}" isShowEdit="{{isShowEdit}}" bind:toIndexActid="toIndexActid" bind:getListData="getListData" lists="{{lists}}"/>
</view>
